<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.mapper.UserRoleMapper">

    <!-- 单条插入 user_role（适用于一对一） -->
    <insert id="insertUserRole" parameterType="com.example.entity.UserRole">
        INSERT INTO user_role (user_id, role_id)
        VALUES (#{userId}, #{roleId})
    </insert>

    <!-- 删除某用户的角色关联（用于更新时先删旧关系） -->
    <delete id="deleteByUserId" parameterType="int">
        DELETE FROM user_role WHERE user_id = #{userId}
    </delete>

    <!-- 可选：按 userId 查询映射记录 -->
    <select id="selectByUserId" parameterType="int" resultType="com.example.entity.UserRole">
        SELECT user_id AS userId, role_id AS roleId
        FROM user_role
        WHERE user_id = #{userId}
    </select>

    <select id="selectRoleIdsByUserId" parameterType="long" resultType="long">
        SELECT role_id
        FROM user_role
        WHERE user_id = #{userId}
    </select>

</mapper>
